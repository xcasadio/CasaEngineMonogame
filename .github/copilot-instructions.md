# Copilot Instructions — CasaEngineMonogame

Les règles de ce dépôt sont dans `AGENTS.md` à la racine, que Copilot lit automatiquement : le lire avant toute tâche.

Les règles par chemin sont dans `.github/instructions/` ; les agents personnalisés dans `.github/agents/` ; les skills, partagés avec Claude Code, dans `.claude/skills/`.
