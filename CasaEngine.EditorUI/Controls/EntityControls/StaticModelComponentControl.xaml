<UserControl x:Class="CasaEngine.EditorUI.Controls.EntityControls.StaticModelComponentControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CasaEngine.EditorUI.Controls.EntityControls"
             xmlns:common="clr-namespace:CasaEngine.EditorUI.Controls.Common"
             mc:Ignorable="d" 
             d:DesignHeight="120" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <common:AssetSelectorControl Grid.Row="0" ValidateAsset="ValidateStaticModelAsset"
                                     AssetId="{Binding StaticModelComponentViewModel.StaticModelAssetId, RelativeSource={RelativeSource AncestorType=local:StaticModelComponentControl}, Mode=TwoWay}" />
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBlock Text="Number of meshes: " />
            <TextBlock Text="{Binding StaticModelComponentViewModel.MeshCount, RelativeSource={RelativeSource AncestorType=local:StaticModelComponentControl}}" />
        </StackPanel>
    </Grid>
</UserControl>
