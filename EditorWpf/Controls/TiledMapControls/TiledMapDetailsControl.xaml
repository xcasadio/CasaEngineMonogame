<UserControl x:Class="EditorWpf.Controls.TiledMapControls.TiledMapDetailsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Map Size" />
        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding MapSize}" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="TileSet File Name" />
        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding TileSetFileName}" />

    </Grid>
</UserControl>
